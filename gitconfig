[color]
	ui = true
[user]
	name = Nathanael Beisiegel
	email = pknb.dev@gmail.com
[core]
	excludesfile = /Users/nathanael/.gitignore
	pager = delta
[push]
  default = simple
[branch]
  autosetuprebase = always
[diff]
	# tool = vimdiff
	colorMoved = default
[delta]
  navigate = true  # use n and N to move between diff sections
	line-numbers = true
[interactive]
  diffFilter = delta --color-only
[merge]
	conflictstyle = diff3
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[mergetool]
	prompt = false
[init]
	defaultBranch = main
