# zsh
npm install -g pure-prompt   # https://github.com/sindresorhus/pure

# Installing with npm rather than brew to easily replace / change version
npm install -g yarn

# Git improvements necessary to work with gitconfig
npm install -g git-open      # adds git open to easily open remote github
npm install -g git-recent    # adds git recent to show branches
npm install -g diff-so-fancy # improves output of diff (via config)

# Useful binaries
npm install -g trash-cli     # use trash instead of rm for safety
